# OpenAI API key for ChatGPT integration
# Replace the value below with your real key. Keep this file secret and do NOT commit it to version control.
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Optional: other env variables can go here
# EXAMPLE_VAR=value
OPENAI_MODEL=gpt-5-mini
